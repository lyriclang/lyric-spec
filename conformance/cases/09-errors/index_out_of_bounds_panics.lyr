//! run
//! panic: LYR-VM0006
// Appendix A: an element index is a runtime value — a negative one panics like one past the
// end, at the access.
fn main(): int {
    let xs = [1, 2, 3];
    let i = 0 - 1;
    return xs[i];
}
